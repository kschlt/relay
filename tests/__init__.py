"""The Relay intake contract suite.

Runnable with the standard library alone: ``python3 -m unittest discover``.
No test framework to install, because an adapter author in another language
should be able to clone this repository and run the contract against their own
output without adopting a Python toolchain first.
"""
